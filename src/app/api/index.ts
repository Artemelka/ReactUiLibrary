import { getAllDictionary, getLabels, getLocales } from './localization';
export { mockAllRequest } from './mock';

export const API = {
    localization: {
        getAllDictionary,
        getLabels,
        getLocales
    }
};
