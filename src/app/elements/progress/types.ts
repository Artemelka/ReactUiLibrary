export interface ProgressCircularProps {
    darkColor?: boolean;
    percent: number;
    strokeWidth: number;
    radius: number;
}

export interface ProgressLinearProps { percent: number; }

export interface ProgressTextProps { value: number; }
